# クイックスタート: Vercel & Railway デプロイ

## 🚀 5分でデプロイ

### Step 1: GitHubにプッシュ（2分）

```bash
cd "/Users/yukio/Downloads/SPCさん/spc-expense-system"

# リモートを追加（YOUR_USERNAMEを置き換え）
git remote add origin https://github.com/YOUR_USERNAME/spc-expense-system.git

# プッシュ
git push -u origin main
```

### Step 2: Vercelでフロントエンドをデプロイ（2分）

1. https://vercel.com にアクセス
2. 「Sign Up」→「Continue with GitHub」
3. 「Add New...」→「Project」
4. `spc-expense-system` を選択
5. **Root Directory**: `apps/frontend` に設定
6. 「Deploy」をクリック

### Step 3: Railwayでバックエンドをデプロイ（3分）

1. https://railway.app にアクセス
2. 「New Project」→「Deploy from GitHub repo」
3. `spc-expense-system` を選択
4. 「+ New」→「Database」→「Add PostgreSQL」
5. 「+ New」→「GitHub Repo」→`spc-expense-system` を選択
6. **Root Directory**: `apps/backend` に設定
7. 「Variables」タブで以下を設定：
   - `JWT_SECRET`: `openssl rand -base64 32` で生成
   - `CORS_ORIGIN`: VercelのURL（例: `https://xxx.vercel.app`）
   - `DATABASE_URL`: PostgreSQLサービスの「Variables」からコピー

### Step 4: 環境変数を更新（1分）

**Vercel**:
- `NEXT_PUBLIC_API_URL`: RailwayのバックエンドURL（例: `https://xxx.railway.app/api/v1`）

**Railway**:
- `CORS_ORIGIN`: VercelのURL

### Step 5: データベースマイグレーション（1分）

Railwayのバックエンドサービスの「Deployments」で：
1. 最新のデプロイメントをクリック
2. 「View Logs」で以下を実行：
   ```bash
   railway run npm run migrate
   railway run npm run seed
   ```

## ✅ 完了！

詳細は `Vercel_Railway接続手順.md` を参照してください。
